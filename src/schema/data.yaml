$schema: http://json-schema.org/draft-04/schema
type: object
properties:

  todos:
    type: object
    patternProperties:
      ^([a-zA-Z0-9]+)$:
        type: object
        properties:
          name:
            type: string
          completed:
            type: boolean
        required: [name, completed]
